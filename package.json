{
  "name": "dev-eagle",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "options": "json-to-scss options.json src/scss/Settings/options/_options.scss --s='!default'",
    "dev": "npm run options && vite",
    "build": "npm run options && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "json-to-scss": "^1.6.2",
    "sass": "^1.57.1",
    "vite": "^4.0.0",
    "vite-plugin-static-copy": "^0.13.0"
  }
}
